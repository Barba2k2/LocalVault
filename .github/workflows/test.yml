name: Test

on:
  pull_request:
    branches:
      - develop

permissions:
  contents: read

jobs:
  test:
    name: Swift tests
    runs-on: macos-15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show toolchain
        run: xcodebuild -version

      - name: Run tests
        run: >-
          xcodebuild test
          -project LocalVault.xcodeproj
          -scheme LocalVault
          -destination 'platform=iOS Simulator,name=iPhone 15 Pro'
          CODE_SIGNING_ALLOWED=NO
          CODE_SIGNING_REQUIRED=NO
